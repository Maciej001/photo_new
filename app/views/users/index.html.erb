<% provide(:title, "All users") %>
<div class="container">
	<h1>All users</h1>
	<section class="users-index">
		
		<%= will_paginate @users, :previous_label => "Previous", 
		:next_label => "Next" %>

		<ul>
			<%= render @users %>
		</ul>

		<%= will_paginate @users, :previous_label => "Previous", 
		:next_label => "Next" %>

	</section>
</div>