- provide(:title, "All users") 

.container
	%h1 All users
	
	.users-index
		= will_paginate @users, previous_label: "<", next_label: ">" 

		%ul 
			= render @users 

		= will_paginate @users, previous_label: "<", next_label: ">" 